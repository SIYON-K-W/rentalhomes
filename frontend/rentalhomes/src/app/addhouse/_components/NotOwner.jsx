const NotOwner = async () => {
	return (
		<div className="h-screen flex justify-center items-center">
			<h4 className="font-semibold text-2xl">only owner can add house</h4>
		</div>
	);
};

export default NotOwner;
